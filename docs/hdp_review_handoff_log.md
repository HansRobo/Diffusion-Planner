# HDP Implementation Review — live progress / handoff log

**Last updated:** 2026-07-06 (session in progress — updated as each verifier agent completes)
**Author:** Claude (max-effort code review), for bin.wang@tier4.jp
**If you are a continuation agent: read this file fully, then jump to "STATE / NEXT STEPS" at the bottom.**

## Task (verbatim user intent)
1. Review the HDP implementation in `/mnt/nvme/Diffusion-Planner-hyper-diffusion-planner`, branch `feature/hyper-diffusion-planner` — find ALL bugs and improvements, no matter how minor, write into a document. Don't miss any detail.
2. User follow-up: "original dp may also be buggy, not only the hdp part, you should also check" — scope includes the pre-existing Diffusion-Planner code in that repo.
3. Final deliverable: a findings document → target path `/mnt/nvme/Diffusion-Planner-hyper-diffusion-planner/docs/hdp_implementation_review_20260706.md` (not yet written; write it from this log when verification completes).

## Review scope & artifacts
- Review target = base commit `4956206` → working tree (2 HDP commits `dbee266`, `6b20ca3` + ~15 files of UNCOMMITTED changes). Working tree is authoritative.
- Full diff saved: `/tmp/claude-1000/-mnt-nvme-Diffusion-Planner/748d0c87-b87d-4c0a-8f09-b0040763766a/scratchpad/hdp_full.diff` (1948 lines)
- Raw finder outputs (JSON): `/tmp/claude-1000/-mnt-nvme-Diffusion-Planner/748d0c87-b87d-4c0a-8f09-b0040763766a/scratchpad/candidates/*.json` (reuse, simplification, efficiency, altitude, angle_a_linescan, angle_b_removed, angle_c_crossfile, angle_d_torch, angle_e_plumbing, paper_math, base_train_loop, base_data_pipeline, base_diffusion_math; base model-path results only in this log below)
- Process: 14 finder agents (10 HDP-diff angles + 4 base-code audits) → dedup → 9 verifier agents (V1–V9) → gap sweep → document.

## Key established facts (do NOT re-derive)
- **HDP paper identified**: arXiv 2602.22801 "Unleashing the Potential of Diffusion Models for End-to-End Autonomous Driving" (Hyper-Diffusion-Planner). Official reference code VENDORED at `reference/external/Hyper-Diffusion-Planner/HDP-nuplan`.
- **Math core verified SOUND** (paper/math agent, numeric checks): VPSDE_linear marginal_prob/marginal_alpha/marginal_prob_std consistent (α²+σ²=1 to 1e-10); all 16 `sde.transform` src→tgt identities correct; v-parameterization formulas correct; DiT score division + ‖s·σ+ε‖² loss = standard ε-loss; NoiseScheduleVP (β 0.1/20, t0=1e-3) matches VPSDE_linear; correct model_type passed to dpm model_wrapper. x_start→x_start round-trip is BENIGN (~1.5e-4 relative shrink, not catastrophic) — waste only.
- **Implementation is paper-FAITHFUL on**: velocity=torch.diff w/ heading copied; ego-only velocity; hybrid loss L_v + ω·L_wpt, ω=planning_hybrid_loss=0.01 (official default); detach-window integral == official Algorithm 1; exp(β·group-normalized reward) with zero-variance-group discard == official. Fork even FIXES an official-code bug (official double-transforms score before hybrid integral).
- **normalization.json ego_velocity [0,0,0,0]/[0.5,0.5,1,1] copies official release verbatim** (lateral under-weighting is a monitoring note, not a deviation).
- Angle-D refutations: in-place ops (`dpm_loss[:,0,:]=`, `*=` on loss slices, `_detached_integral` roll+fill, `_latent_to_prediction` slice write) are autograd-safe; `ddp.setup` calls `torch.cuda.set_device` so map_location='cuda' lands per-rank (memory spike claim still valid, wrong-device claim not).
- Angle-E refutations: `Diffusion_Planner.sde` DOES delegate to decoder sde (fallback identical); schedules consistent.
- No CLAUDE.md in HDP repo (conventions angle void). No unit tests for HDP core (velocity codec, sde.transform, hybrid loss, reward weighting) — test gap for doc.
- `_train_config_default` exists in train_predictor.py at base (not a missing symbol).

## CONSOLIDATED FINDINGS (dedup'd; verdicts updated live)

### A. HDP-diff correctness — verified so far
| # | Finding | Verdict | Severity |
|---|---------|---------|----------|
| A1 | grpo_epoch.py:70-76 `_sft_step` omits `planning_hybrid_loss * ego_planning_hybrid_loss` term that train_epoch.py:82-83 adds; RL steps DO include it (grpo_utils.py:382). SFT anchor steps in GRPO runs never backprop waypoint objective. Found independently by 7 angles. | **CONFIRMED** (V1) | high |
| A2 | grpo_epoch.py:87 checks key `ego_hdp_velocity_loss` but decoder emits `ego_hdp_diffusion_loss` (decoder.py:255) → SFT metric never logged; silent; wandb series averages GRPO steps only. | **CONFIRMED** (V1) | low |
| A3 | train_epoch.py:81-83 SFT ego weighting = alpha:omega, RL = 1:omega (grpo_utils.py:382) → diverges when alpha_planning_loss≠1 (default 1.0). | **CONFIRMED** (V1) | low |
| A4 | train_epoch.py:108 wandb comprehension guard is a no-op filter; epoch/global_step/lr each double-logged. Cleanup-only. | **CONFIRMED** (V1) | low |
| A5 | grpo_epoch.py:169-212 constant-zero placeholder metric series (abs_advantage in official mode; official_weight_* in grpo mode) flow to wandb. Metrics-only. | **CONFIRMED** (V1) | low |
| A6 | Hypothesis: missing keys KeyError in _grpo_step official branch. | **REFUTED** (V1): grpo_utils.py:415-419 always returns the keys; all branches define both tensors. | — |

### B. Awaiting verifier verdicts (V2–V9 running; candidates below with finder confidence)
**V2 (train.py/schedule/ddp)**: [B1] train.py:432 final-10-epochs LR override crushes runs with train_epochs≤15 (high). [B2] ModelEma constructed BEFORE resume/init-weights load + bare-except on missing ema_state_dict → EMA seeded random (medium; check load_weights_only order too). [B3] --use_ema false → NameError model_ema (high). [B4] lr_schedule.py LinearLR total_iters=warm_up_epoch-1; warm_up_epoch=1 → 0.1x LR forever; no cosine phase despite name (medium). [B5] --coeff_timestep argparse type=list → unusable flag (high; also train_grpo_predictor:287). [B6] ddp.py:20 overwrites MASTER_ADDR/PORT unconditionally → multi-node broken, port collisions (medium). [B7] find_unused_parameters default False in train.py vs hardcoded True in train_grpo_predictor (low). [B8] closed_loop_validate logs step=epoch+1 after stepless auto-step advanced → wandb DROPS all closed-loop rows (high); resume restarts _wandb_global_step at 0 (medium).

**V3 (config/normalizer defaults)**: [C1] decoder.py:355-358 reads config.diffusion_sample_steps (+use_velocity_representation) with no default; utils/config.py Config only backfills guidance_scale → AttributeError loading ANY pre-HDP checkpoint via valid_predictor/ROS node/onnx export/rlvr (high; 5 angles). [C2] use_velocity_representation default flip False→True + trainers build model from CLI args not checkpoint args.json → old waypoint checkpoints silently reinterpreted as velocity on resume/warm-start (high). [C3] StateNormalizer.from_json KeyError('ego_velocity') for legacy normalization.json; getattr(args,'use_velocity_representation',False) silently picks waypoint stats where attr missing (high). [C4] ObservationNormalizer absorbs 'ego_velocity' as bogus observation entry serialized into args.json (medium). [C5] encoder.py:174 slice flip [:6]→[-6:] — establish time ordering ground truth + silent warm-start incompat (medium). [C6] getattr(args,'rl_objective','grpo') fallback ≠ TrainConfig default 'official_reward_weighted' (+ other getattr defaults) (medium).

**V4 (export/guidance)**: [D1] onnx_export.py:215-216 TurnIndicatorONNXWrapper: (a) plain mean vs masked mean pooling; (b) raw latent slice vs velocity integration → deployed turn signals garbage in velocity mode (high). [D2] DecoderONNXWrapper exports raw DiT output, no model_type→x_start conversion, no velocity integration → deployment contract broken (high). [D3] composer.py:45-49 + guidance_wrapper.py:59-64 guidance assumes x_start + waypoint rows → broken for velocity ego row AND noise/score/v (high; check reachability/guidance defaults). [D4] collision.py:118 CollisionGuidance.reward() calls autograd.grad under no_grad → always RuntimeError (high, base bug). [D5] guidance_wrapper.py:76 NaN-energy fallback zeros without grad → RuntimeError aborts planning (medium, base). [D6] decoder.py:437 delay/prefix contract in velocity mode: prefix waypoints pinned into velocity latent → output[:delay] ≠ prefix + offset corruption (medium). [D7] dpm_solver:512 timesteps_masked no-op (t_masked==t) → prefix time-pinning has no inference counterpart (low, base).

**V5 (decoder loss math)**: [E1] turn-indicator input = waypoints normalized by VELOCITY std 0.5 → ~±240 vs ±4; consistent train/inference but ill-conditioned + breaks warm-started heads (high; 4 angles). [E2] prefix columns for noise/v/score supervision target never-applied z (+~95x sigma amplification for cross-parameterization at t=eps) → irreducible gradient noise in first ≤5 ego steps (high per paper agent). [E3] pred_x_start 1/alpha ~150x amplification at t~1 for noise/score/v feeding always-on hybrid waypoint loss + penalties, no SNR weighting (medium-high). [E4] velocity-branch neighbor x_start supervision uses UNMASKED waypoint_gt vs masked all_gt elsewhere — check downstream masking makes it harmless (open question). [E5] ego_velocity lateral under-weighting (monitoring note). [E6] decoder.py:88 longitudinal_velocity read AFTER observation normalization (std 20) → coeff_velocity high-speed lon-loss attenuation is a no-op below 20 m/s raw (medium, PRE-EXISTING base bug).

**V6 (grpo_utils RL)**: [F1] compute_official_reward_weights: batch-normalize NaN poisoning (pre-filter mean/std); valid_sample gated on per-group std even for normalize='batch'/'none' → identical-reward groups dropped though exp(0)=1 is paper-correct there; n=1 → NaN → silent no-op RL (medium). [F2] compute_group_advantages zero-variance/n=1 handling (check). [F3] sample_group xT init noise*U[0,grpo_noise_scale=3] vs VP marginal N(0,1); also validate_model zeros init → train/eval skew (medium). [F4] compute_gt_l2_distance: stationary-ego GT ≈(0,0) treated as padding → ADE=0 → realism penalty vanishes in stop scenes (medium). [F5] ego_pseudo_gt frame check (ego-centric? origin-prepend correctness; 'world frame' docstring). [F6] velocity-mode ego time-0 slot consistency between decoder and grpo_utils latent construction.

**V7 (data pipeline)**: [G1] data_augmentation.py:195/613 interpolation_future_trajectory 3-col-only, runs unconditionally → 4-col futures CRASH with aug enabled (2 angles; must reconcile with team actually running training — check aug flags/format in live launchers) (high). [G2] centric_transform never transforms goal_pose (also bridge variant) → model trained to mistrust goal token under aug (high, BASE bug). [G3] parse_rosbag.py:386 + cpp neighbor_processor.cpp:161 neighbor future deque seeded with current frame → short-tracked neighbors' GT shifted 1 step late in ALL training npz (high, BASE). [G4] parse_rosbag ego arrays float64 (medium). [G5] pose_padding_mask after heading_to_cos_sin can't detect padding (low). [G6] ObservationNormalizer.inverse mask on normalized data (low). [G7] bridge aug 3-col corruption of 4-col futures (medium).

**V8 (base model path)**: [H1] validate_model.py:252 + other entry points: xT init = ZEROS not N(0,1) → off-distribution, deterministic sampling; GRPO/DPO group sampling degenerate unless grpo_noise_scale; torch2onnx uses 0.5*randn (inconsistent) (medium-high, BASE). [H2] = E6 longitudinal_velocity. [H3] decoder.py:435 inference output rows for invalid neighbors = denormalized garbage, not zeroed (known upstream bug; loss side fixed, output side NOT) (medium, BASE). [H4] dit.py:123 hardcoded T=81 vs config.future_len (low). [H5] decoder.py:511 flow_matching inference always crashes (3D x into DiT assert dim==4) (high but unused branch?, check if pre-existing). [H6] encoding_pooled masked-mean all-masked division + encoder output masking commit 37e511d presence (open).

**V9 (rlvr compat + cleanup facts)**: [I1] rlvr/grpo_loss.py, grpo_sft_trainer.py, preference_optimization/dpo_loss.py build waypoint-space ego latents → corrupt velocity checkpoints silently (high). [I2a-j] cleanup fact checks: dead code (diffusion_prediction_target/_to_x_start; hybrid_loss), l_v discarded+duplicate compute, unused current_states param, inline inverse-normalize dup at decoder.py:269, _train_config_default dup, 3 expand helpers, marginal_prob(ones) waste, wandb dict build guard placement, sample_steps 25 vs official 10 (~2.4x inference cost). [I3] load_weights_only prefix logic + strictness + torch.load hygiene. [I4] epoch-0 wandb block guards.

### C. Cleanup/efficiency findings (from finders; mostly cross-confirmed, V9 fact-checking the load-bearing claims)
1. ~90-line duplication of the entire VP training-loss path between decoder.compute_training_loss and grpo_utils._compute_policy_ego_loss_per_sample (supervision ladder ×4 across 2 files) — highest-value refactor: extract shared helper; copies ALREADY diverge (planning_hybrid_loss application point, A1/A3).
2. Dead code: loss.py diffusion_prediction_target + diffusion_prediction_to_x_start (0 callers, duplicate sde.transform); loss.py hybrid_loss (0 callers).
3. hybrid_loss_components computes l_v discarded by both callers; for supervision=x_start it exactly duplicates ego_diffusion_loss computed 3 lines above.
4. sde.transform lacks src==tgt short-circuit (default path does identity via noise round-trip; ~1e-4 bias + ~10 wasted kernels ×2 per step).
5. normalize_ego_state/inverse_normalize_ego_state: per-call .to(device) H2D copies in hot loop (≥6/step train, 6-8/inference tick); should cache buffers. Also duplicates StateNormalizer logic.
6. diffusion_sample_steps default 25 vs official HDP reference 10 → ~2.4x inference/rollout/deployment cost, unjustified.
7. _turn_indicator_trajectory_from_latent + _latent_to_prediction duplicate ego integration per inference call; _latent_to_prediction inverses full latent then overwrites ego row.
8. waypoint_gt.clone() [B,33,81,4] ~22MB/step; marginal_prob(torch.ones_like) materializes full alpha tensor (use marginal_alpha); dpm_loss zeros+overwrite vs cat.
9. load_weights_only map_location should be 'cpu' (per-rank GPU spike).
10. grpo_epoch zeros_like scaffolding → use .get at log site.
11. diffusion_time_sample_method speculative generality (5 plumbing sites, 1 option).
12. _latent_to_prediction unused current_states param; decoder.py:269 inline re-implementation of inverse_normalize_ego_state.
13. expand_dim/reshape_time/expand_dims triplication; marginal_alpha copy-paste of mean_log_coeff.
14. _train_config_default + boolean() duplicated across both trainers.
15. train.py epoch-0 wandb block duplicates per-epoch dict; wandb double-logged keys (A4).
16. Altitude: ego-row-special-casing hand-enforced at every touch point (decoder ×4, grpo_utils, onnx wrapper MISSED, guidance MISSED) → argues for a LatentCodec/normalizer-owned encapsulation; StateNormalizer dual semantics needs explicit representation tag; turn_indicator_trajectories silent fallback to gt_trajectories only correct in waypoint mode; _wandb_global_step/_current_epoch smuggled through args.

### D. Base-repo (original DP) findings summary (verdicts pending in V2/V4/V5/V6/V7/V8)
LR final-phase override; EMA-before-load; use_ema NameError; lr warmup bug; coeff_timestep argparse; ddp MASTER_ADDR; goal_pose aug omission; neighbor-future misalignment (python+C++ converters); float64 npz; CollisionGuidance.reward no_grad crash; NaN-energy fallback crash; zeros xT init; timesteps_masked no-op; longitudinal_velocity normalization no-op; inference neighbor rows unmasked; flow_matching inference crash; DiT T=81 hardcode; gt_l2 stationary hole; pose_padding_mask post-cos/sin; ObservationNormalizer.inverse mean-collision.

## STATE: REVIEW COMPLETE (2026-07-06)
**Final document written: `docs/hdp_implementation_review_20260706.md`** — 1 critical, 12 high, 15 medium, ~21 low, 16 cleanup items, verified-sound/refuted appendix, test recommendations.
Gap sweep returned 2 new findings, both verified inline: (1) train_grpo_predictor.py:650 `agg.get("epdms_total", 0.0)` — key never produced by aggregate_valid_metrics (only epdms_means) → constant-0.0 wandb series; (2) train_grpo_predictor.py:598/:373 — GRPO wandb project silently changed from hardcoded "Diffusion-Planner-GRPO" (base :538) to default "Diffusion-Planner".
Nothing remains to do for this review. Steps below kept for historical reference.
1. **DONE**: 14/14 finders. **DONE**: ALL 9 verifiers (V1-V9) — verdict tables above are COMPLETE and are the authoritative findings list.
2. **DONE**: Phase-3 gap-sweep (results above).
3. **THEN (main remaining work)**: Write final document `/mnt/nvme/Diffusion-Planner-hyper-diffusion-planner/docs/hdp_implementation_review_20260706.md` from the verdict tables above. Structure: Executive summary (HDP core math verified SOUND + paper-faithful; issues are integration/plumbing, defaults, and pre-existing base bugs) → Critical (C2 default flips) → High (A1 SFT hybrid omission, C1 Config AttributeError, D1 ONNX turn wrapper, D3 guidance velocity, I1 rlvr corruption, B1 LR override, B3 use_ema NameError, B8 wandb closed-loop drop, E2 prefix z-target, E3 1/alpha landmine, G2 goal_pose aug, G3 neighbor future misalignment) → Medium → Low/minor (all confirmed low items incl. cleanup) → Pre-existing base-DP bugs section (E6/H1/H3/H4/H5/B*/G* base items) → Cleanup/efficiency/altitude (section C of this log + I2a-j) → Test-coverage gaps & recommended tests → Appendix: verified-sound + refuted list (V1#6, B7, E4, F2, F5, F6, H6, I3a-c, I4, C6, C3-silent-subclaim; sde math, schedules, transforms all verified numerically).
4. User cares about: EVERY detail incl. minor; both HDP diff AND base code; document as deliverable. Do NOT fix code — report only.
5. After doc: reply to user with TLDR + doc path. Optionally update memory (/home/ubuntu/.claude/projects/-mnt-nvme-Diffusion-Planner/memory/): new memory for HDP repo review results linking [[base-model-tier4main-bugs]]; note NEW empirically-verified base bugs that ALSO apply to main repo: neighbor-future one-step misalignment (747/747 in live corpus), goal_pose aug omission, coeff_velocity normalization no-op (verified present in sibling /mnt/nvme/Diffusion-Planner too).

## POST-REVIEW: fix verification round (2026-07-06 evening, in progress)
Another agent applied fixes (all uncommitted). Isolated fixer diff: `<scratchpad>/fixer.diff` (883 lines, 18 files; reconstruct reviewed state via `git worktree add <dir> 4956206 && git apply hdp_full.diff`).

**Fixes observed in round 1 (read in full, mapping to findings):** C-2 defaults reverted (use_velocity=False, rl_objective=grpo, sample_steps=10) + NEW assert_checkpoint_compatible on resume/init (train.py+grpo trainer); H-1/H-2 (compose_supervised_total_loss shared; Config defaults table); H-3 (ONNX wrapper → _pool_encoding + _turn_indicator_trajectory_from_latent); H-4 guarded (RuntimeError if guidance with velocity/non-x_start); H-6 fixed by masking prefix cols out of ego loss (decoder + grpo_utils) AND velocity now hard-restricted to x_start/x_start (NotImplementedError) which also guards H-7; H-8 (LR override gated train_epochs>10); H-9 (model_ema=None pattern + ema None-guards everywhere); H-10 (closed_loop wandb stepless); H-12 C++ fix (neighbor seed removed — python parse_rosbag.py NOT fixed); M-2 (resume_model ema None-guard + explicit KeyError, still prints-only); M-3 (parse_float_list); M-4 (ddp setdefault); Low-1 (key renamed ego_hdp_diffusion_loss); Low-5 (epdms_means["total"]); Low-7 (lr_schedule warm_up<=1 → fixed_scheduler); G-1/G-7 (4-col support in interpolation + bridge, goal_pose transform added to BOTH aug classes = H-11); efficiency: load_weights_only map_location=cpu, sde.transform src==tgt short-circuit.
**⚠ Deviation introduced:** planning_hybrid_loss default 0.01→0.05 (official HDP ω=0.01) — no justification found.
**NOT touched in round 1:** rlvr/preference stacks (H-5 — still unguarded), parse_rosbag.py python converter (H-12 python side, float64 G-4), guidance semantic fix (guarded instead — acceptable), collision.py no_grad landmine (M-7), loss.py dead code, stationary-GT/5-step reward holes (M-11), init-distribution skew (M-10), D-2 split-graph contract, sweep-2 wandb project default.

**Round 2 (user-forwarded): split-scale redesign** — StateNormalizer row0 = waypoint stats ALWAYS; new ego_velocity_mean/std fields + normalize/inverse_normalize_ego_velocity used ONLY for the ego diffusion latent; turn head/waypoint_gt/output normalization back to waypoint scale; legacy fused-scaling velocity checkpoints refused (train.py guard + RuntimeError in _ego_velocity_stats).
**My verdict: split-scale is the RIGHT design for this fork** (better than official fused): official is ego-only with no turn head/deployment contract so fused was harmless THERE; this fork has aux head + args.json contract → fused was the root cause of M-1/E1 and the StateNormalizer dual-semantics altitude finding. Ego diffusion channel math unchanged vs official (velocity latent still ego_velocity stats) → paper fidelity preserved. Smoke tests PASSED (py_compile 6 files; from_json row0=[20,20,1,1] + ego_velocity_std=[.5,.5,1,1]; velocity norm round-trip; codec round-trip; turn-head feature max 5.5 for 120m straight ✓ sane; legacy normalizer raises loudly; to_dict carries new fields).
**Caveats flagged to user:** (a) planning_hybrid_loss 0.05 vs official 0.01 — revert unless deliberate; (b) legacy velocity ckpts fail LATE on eval paths (first inference RuntimeError) — add same check in Config.__init__; (c) H-5 rlvr now FAILS QUIETLY (scales comparable post-split, no 40× signal) — guard there is now more urgent; (d) args.json state_normalizer gains ego_velocity_mean/std keys — cpp/ROS confirmed not parsing args.json (V3), ok.
**REMAINING for full double-check:** deep-verify round-1 fix correctness (esp. prefix-mask ego-loss denominators B vs B,T reduction; assert_checkpoint_compatible path candidates; compose loss in _sft_step w/ grpo args having alpha fields; bridge 4-col cos/sin round-trip under smoothing; interpolation 4-col heading conversion at aug boundary) + fresh-bug hunt on fixer.diff + confirm not-touched list with user.

## FIX DOUBLE-CHECK COMPLETE (2026-07-06 night; verification agents killed by org spend limit → completed inline)
Round 3 discovered: NEW utils/hdp_compat.py (require_waypoint_diffusion / require_velocity_normalizer) + guards at ALL rlvr (grpo_loss, grpo_logprob_loss, grpo_sft_trainer, train_grpo) and preference_optimization (dpo_loss, train_dpo) entry points → H-5 GUARDED (loud NotImplementedError).
**Verified FIXED/GUARDED inline:** C-2 (defaults reverted + compat guard); H-1 (compose helper, grpo args complete, term-for-term equal); H-2 (Config defaults incl. round-2 velocity fields); H-3 (ONNX wrapper → decoder helpers; python-bool branching = traceable); H-4 GUARDED (RuntimeError guidance+velocity/non-x_start); H-5 GUARDED (round 3); H-6 FIXED (prefix cols masked out of ego loss; indexing (B,P,T+1,1)[:,0,1:1+H,0] vs dpm_loss[:,0,:H] verified aligned; delay=0 reduces to old mean); H-7 GUARDED (velocity hard-restricted to x_start/x_start); H-8 (gated train_epochs>10); H-9 (model_ema None pattern, all refs in both trainers guarded); H-10 (closed_loop stepless; grpo run internally step=consistent); H-11/G-2 FIXED both aug classes (vector_transform rank-2 (B,2) safe: nexpand=0→bmm(B,2,1); zero-mask restore correct); H-12 C++ FIXED (seed removed; as_array insertion-order-from-front + zero tail; empty history → all-zero padding row; python parse_rosbag NOT fixed — live corpus is C++-produced); M-2 (None-guard + explicit key check; still print-only on real load failure = accepted); M-3 (parse_float_list handles comma/space/bracket, enforces 4); M-4 (setdefault; note --port ignored under torchrun now, torchrun's env wins — fine); M-9 FIXED and now MATCHES official (zero-variance groups kept at weight exp(0)=1; batch mode finite-filtered; n=1 → weight-1 BC instead of silent no-op — behavior change, defensible); M-13/G-1/G-7 (4-col interpolation+bridge); M-15 (steps 10); Low-5 FIXED ("total" key exists: pdms_proxy.py:325); Low-7 (warm_up<=1 → fixed_scheduler); split-scale round 2 (smoke-tested earlier); sde.transform short-circuit (aliasing safe: no downstream in-place mutation of pred tensors); StateNormalizer signature change safe (only config.py constructs it).
**OPEN items after all 3 rounds:** (items 1-4 RESOLVED in round 4 — see below)
1. ~~Metric key split~~ 2. ~~compat guard no override~~ 3. ~~planning_hybrid_loss 0.05~~ 4. ~~Config late failure~~

## ROUND-4 FIXES VERIFIED (2026-07-06, all 4 open items resolved; behavior smoke-tested)
1. **Metric keys unified**: ego_hdp_diffusion_loss/ego_hdp_waypoint_loss now emitted consistently by decoder(:270), _sft_step(:82-85), _grpo_step(:202-203), and all 3 grpo_utils return dicts (:440-441,:489-490,:533-534); ego_hdp_velocity_loss = zero grep hits repo-wide. ✔
2. **Warm-start policy**: assert_checkpoint_compatible gained keyword-only allow_waypoint_to_velocity_init; train.py hardwires True for init_weights_path (:306-310), strict for resume (:304) and GRPO resume (train_grpo_predictor:466). Direction-restricted: ONLY waypoint→velocity allowed (velocity→waypoint refused even with flag). Smoke-tested 4 scenarios: init-allowed-with-warning ✔ / resume-refused ✔ / wrong-direction-refused ✔ / legacy-fused-refused ✔.
3. **planning_hybrid_loss default back to 0.01** (official ω). ✔
4. **Config.__init__ now calls require_velocity_normalizer early** (utils/config.py imports hdp_compat; raises informative RuntimeError on legacy fused velocity args.json before model construction) + validates state_normalizer dict presence. Smoke-tested ✔.
py_compile of all touched files OK. Working tree is now clean of all review-critical items; remaining known-unfixed list (base-repo/lower priority) unchanged from FIX DOUBLE-CHECK section item 5.

## ROUND-5: PERF-AFFECTING RESIDUALS FIXED (2026-07-06, by review agent; user asked to fix everything perf-relevant however small)
Classification of the remaining open list by model-performance impact → 5 mechanical fixes applied + 1 behavior-preserving semantic fix; 2 items deliberately deferred to experiments; 4 items confirmed non-perf (hygiene only).
**Applied (all py_compile + behavior smoke-tested):**
1. ros_scripts/parse_rosbag.py — neighbor-future deque seed removed (mirrors C++ fix; python-generated npz no longer 1-step-late for short-tracked neighbors); ego arrays now float32 at allocation AND cast at save. [data quality]
2. grpo_utils.compute_gt_l2_distance — all-zero ego GT (stationary ego, not padding) now falls back to unmasked ADE: stop scenes keep the w_gt_l2 realism anchor. Test: lunging sample vs stationary GT now scores ADE 4.05 (was 0.0); moving-GT case bit-identical. [RL reward]
3. loss.compute_neighbor_collision_penalty gained eval_steps param (default = legacy _NEIGHBOR_EVAL_STEPS → validation/training-loss paths bit-identical); grpo_utils passes new _RL_COLLISION_EVAL_STEPS = every 5th step + final (17 steps): closes the between-steps collision blind spot in the RL reward. Test: synthetic hit at t=8..11 → default penalty 0.0 (blind), dense 2.0 (caught); same-steps call bit-identical. [RL reward]
4. model/guidance/collision.py reward() — internal autograd wrapped in torch.enable_grad() on a re-leafed x_padded, result detached: no longer guaranteed-RuntimeError under the @no_grad decorator. Smoke: runs, detached [B]. (Surrogate semantics unchanged/pre-existing.) [RL reward capability]
5. coeff_velocity semantic fix, EXACTLY behavior-preserving: decoder.py + grpo_utils.py now denormalize the lon-velocity channel to m/s via new ObservationNormalizer.stats(); defaults changed 1.0 → 0.05 (= 1/std20) in TrainConfig + both trainers (verified |v_norm·1.0| == |v_raw·0.05| exactly; NO launcher passes --coeff_velocity explicitly → global behavior preservation). The knob now honestly weights physical speed; raising it activates the documented high-speed lon-loss attenuation. [dead knob made real, zero behavior change]
**Deliberately deferred (perf-relevant but requires experiments, not mechanical fixes):**
- Eval/deployment zeros xT init + GRPO rollout randn·U[0,3] skew (M-10/H1): changing either alters eval comparability & GRPO exploration; official HDP uses consistent randn·0.5 everywhere. Recommend closed-loop A/B (per team practice: judge via closed-loop probes, not valid_loss) before aligning the three init distributions.
**Confirmed non-perf, left open:** loss.py dead code; GRPO wandb project default; SFT/RL supervision-ladder duplication; DecoderONNXWrapper contract docs.

## ROUND-6: NON-PERF HYGIENE CLOSED + COMMITS (2026-07-07)
All four remaining hygiene items done, verified BIT-IDENTICAL where they touch live paths:
1. loss.py dead code deleted (diffusion_prediction_target, diffusion_prediction_to_x_start, hybrid_loss — re-verified zero callers).
2. Supervision-ladder dedup: new loss.vp_supervision_elementwise_loss shared by decoder + grpo_utils (smoke: bit-identical to pre-refactor score/noise/v formulas); velocity branches simplified to x_start-only (the entry guards make the other arms unreachable) and the zeros+overwrite dpm_loss replaced by torch.cat (bit-identical, also removes the reviewed inefficiency).
3. GRPO wandb project default restored to "Diffusion-Planner-GRPO" (sweep-2).
4. DecoderONNXWrapper: contract documented + constructor refuses velocity/non-x_start; build_wrappers/build_export_specs skip ONLY the split decoder graph for such models (FullONNXWrapper still exported; training auto-export unaffected — uses _guarded anyway). ModelWrappers.decoder now Optional.
Whole working tree committed in 7 thematic groups (see git log). Remaining decisions intentionally left open: eval/rollout init alignment (needs closed-loop A/B).

## ROUND-7: LIVE-RUN PATH AUDIT (2026-07-07, run zx8l6fmi / hdp_velocity_hybrid_omega001_..._fixed_rerun2)
Runtime evidence + fresh full re-read of the exact running path (velocity x_start/x_start, ω=0.01, quintic aug+smoothing, 8-GPU DDP, from-scratch, 60ep, no closed-loop):
- args.json CORRECT: split-scale live (SN row0 std [20,20,1,1] + ego_velocity_std [0.5,0.5,1,1]), coeff_velocity=0.05, rb=1.0, nc=0.0.
- **Loss composition PROVEN on live data**: 0.1·0.4930(nbr) + 1.0·0.0488(ego vel) + 0.01·10.064(hybrid) + 0.0052(ti) + 0.0019(rb) = 0.20590 == logged train_step/loss 0.2058982. ego_hdp_diffusion_loss≡ego_planning_loss, hybrid≡waypoint alias ✓. Magnitudes sane.
- Full fresh read of compute_training_loss (incl. round-5/6 self-edits never independently reviewed): consistent — guards, split-scale, prefix masking indexing, velocity-branch cat refactor, lon-vel denorm (stats("ego_current_state")[4], float scalars), penalty path integrate(inverse_velocity) ✓.
- Validation under velocity VERIFIED correct space: prediction (integrated physical waypoints) vs raw GT; EPDMS proxy consumes physical ego_pred; zeros xT init unchanged (deliberate, comparable with history).
- train.log: no NaN/err; epoch-0 validation completed; ~3.1 batch/s.
- turn_indicator_accuracy=0.000 at epoch-0 EXPLAINED: untrained constant-argmax head vs KEEP-dominated GT — init artifact, not a bug; expect it to climb.
- Watch points ahead: first checkpoint save + guarded ONNX export at epoch 10 (velocity → split decoder graph skipped by design, full graph exported); LR final-phase override engages at epoch 50 (intended for 60ep runs); monitor hybrid-vs-velocity term ratio (ω=0.01 official; hybrid currently ~2× the velocity term).
**Verdict: no known correctness defect on the running path. Remaining unknowns are hyperparameter-level (ω ratio, lateral weighting) and deferred decisions (eval init) — measurable only via closed-loop probes, not further static audit.**

## ROUND-8: TRAINING THROUGHPUT (2026-07-07, commit ddb0bb9)
Live evidence: GPU util sawtooth 46-100% (~70% avg), 3.1 batch/s (global bs256 = 32/GPU), load avg 13.5 (CPU not saturated). Root causes = CPU-GPU serialization in the loop, NOT the model:
per-step torch.cuda.synchronize(); compute_grad_stats every step (cat of ALL grads + 5×.item() = 5 syncs/step); blocking .to(device); ~20 H2D normalizer-stat copies/step; marginal_prob(ones_like [B,321,80,4]); full-dict observation inverse for 2 needed keys; no persistent_workers/prefetch; epoch_loss holding graph-attached tensors (part of 42GB RSS/proc).
**All fixed numerics-identical (verified bitwise: sde alpha/std swap, normalizer cache; grad stats now sampled on the wandb cadence — diagnostics-only change).** Expected +20-40% throughput; live run zx8l6fmi does NOT pick these up (module already loaded) — restart required to benefit; at ~7.9 days/60ep a restart at epoch≤2 pays for itself if gain ≥~5%.
**Not applied (change numerics/dynamics — separate decisions):** bf16 autocast (est. 1.3-1.8×, needs closed-loop check), bigger per-GPU batch (32 is small for this model; needs LR retune), torch.compile, EMA every-k-steps, num_workers increase (CPU headroom exists).

### ROUND-8b: second wave, live-verified, defaults flipped ON (commit 5d926ff); production relaunched
User approved stopping the run + batch×2. Measured on the 8-GPU node:
- bs512 + bf16 + fused AdamW + static_graph + bucket views: **1193 samples/s vs 794 baseline (+50%), 98% avg GPU util** (was ~70% sawtooth), loss trajectory healthy vs fp32 at equal samples (3.79→3.04 vs old 7.99→5.92 at half the samples).
- Batch sweep: bs768 = 1190 samples/s @ 78.6/80GB (no gain, 1.4GB headroom = OOM risk with EPDMS eval on); bs1024 = OOM at first batch. **bs512 is the knee — compute-bound, not memory-bound; filling VRAM buys nothing here.**
- Worker analysis: npz load p50 4.3ms / p99 5.6ms / max 181ms (RDMA cold tail) vs 107ms/worker budget → 18× headroom; 16 workers/rank sufficient; more workers only bloat RAM (9M-path list COW per persistent worker). Residual 2-5% util gap = per-step python orchestration + DDP 8-rank straggler coupling + rank-0 wandb/tqdm; next lever would be torch.compile/CUDA graphs (experimental, not attempted).
- TF32 remains ON and applies outside the autocast region (bf16 supersedes it inside the model forward only).
- **Production run: outputs/hdp_velocity_hybrid_omega001_base60_fullseq_node01_8gpu_bf16_bs512_rerun3, wandb pfdnaqp8** — bs512, lr 2e-4 (linear scaling from 1e-4@256), workers 16, EPDMS/PDMS on, 60ep ETA ~5.3 days (was ~7.9). Old zx8l6fmi stopped at epoch 1 (user-approved); wandb junk runs khe2w159 (verify) + n2psrn5i (aborted first relaunch) deleted; probes ran wandb-offline (no cloud runs).
5. NOT fixed (known, mostly base-repo/lower priority): python parse_rosbag.py neighbor seed + float64; collision.py reward() no_grad landmine; validate/eval zeros xT init + RL init skew (M-10); stationary-GT + 5-step collision reward holes (M-11); coeff_velocity normalization no-op (M-12 — needs deliberate decision); loss.py dead code; DecoderONNXWrapper split-graph contract docs (D-2); GRPO wandb project default flip (sweep-2); SFT/RL supervision-ladder duplication (compose helper unified only the total-loss composition).

## Verifier agent outputs (raw, appended as they arrive)
### V1 grpo_epoch/train_epoch losses — COMPLETE (see table A)

### V2 train.py/schedule/ddp — COMPLETE
| id | Finding | Verdict | Severity | Key evidence |
|----|---------|---------|----------|--------------|
| B1 | train.py:430-441 final-phase LR override | **CONFIRMED** (boundary: full-run clobber for train_epochs<=10; partial 11-15; current launchers use 60 so today it hits epochs 50-59, plausibly intended — but any short warm-start fine-tune silently trains at 0.1x/0.01x LR; override runs at top of every epoch AFTER warmup scheduler/resume LR set) | high |
| B2 | EMA seeded from random on resume | **CONFIRMED for resume path both trainers** (train.py:314 ModelEma built before resume_model:342; train_utils.py:115-123 bare except swallows missing ema_state_dict AND module.-prefix load errors; poisoned EMA then saved to every checkpoint). **REFUTED for init_weights_path**: load_weights_only (train.py:311-312) runs BEFORE ModelEma:314 — warm-start EMA correctly seeded. Mitigated when ckpt written by these trainers (they always save ema_state_dict). | medium |
| B3 | --use_ema false NameError | **CONFIRMED** train.py only (model_ema assigned only under if args.use_ema:314 but referenced at 342/446-448/511). train_grpo_predictor: no crash — ModelEma unconditional at 563, its --use_ema flag silently ignored (separate wart). | high |
| B4 | lr_schedule warm_up_epoch=1 pins 0.1x + no cosine phase despite class name | **CONFIRMED as latent code behavior** — but no launcher uses warm_up_epoch=1 (defaults 5 / GRPO 2); real impact = misleading name (post-warmup LR constant, no annealing) + footgun. | low |
| B5 | --coeff_timestep type=list | **CONFIRMED** (train_predictor.py:100-105, train_grpo_predictor.py:287): list("1,1,1,1") = 7 chars → assert fails (train.py:202); crafted 4-char '1111' passes assert then TypeError Tensor *= str in loss.py:239-242. Flag unusable in every form. | medium |
| B6 | ddp.py:15-20 torchrun branch unconditionally overwrites MASTER_PORT=args.port, MASTER_ADDR='localhost' before env:// init | **CONFIRMED**: multi-node → non-zero nodes rendezvous against nonexistent local store → hang; single-node concurrent jobs collide on default port 22323 unless launchers thread unique port into BOTH --master_port and --port (current launchers do, e.g. launch.sh 22477 twice). | medium |
| B7 | find_unused_parameters=False unsafe | **REFUTED for train.py supervised default path** — verifier walked every param: one forward per step; turn_indicator head used in forward + loss; all encoder branches unconditional; padding handled via torch.where/multiply keeping params in-graph (zero grads ≠ absent grads); FusionEncoder forces token 0 unmasked; DiT ego row unmasked; validate under no_grad. GRPO hardcoded True is defensible for RL losses. Escape hatch flag exists for non-default configs. | low |
| B8 | wandb step regression drops closed-loop logs | **CONFIRMED (a)**: train.py:409-422 + 478-491 stepless logs advance auto-step BEFORE closed_loop_validate (541) logs step=epoch+1 (193) → auto-step already epoch+2 (or thousands with step-interval logging) → ALL closed-loop scalars + rollout videos silently dropped in standard supervised online runs. GRPO internally consistent. **(b) CONFIRMED-conditional**: _wandb_global_step never checkpointed/restored → restarts at 0 on resume; misordered charts when --wandb_run_id reattaches to existing run. | high |

### V3 config/normalizer defaults — COMPLETE
| id | Finding | Verdict | Severity | Key evidence |
|----|---------|---------|----------|--------------|
| C1 | config.diffusion_sample_steps AttributeError for pre-HDP checkpoints | **CONFIRMED** — utils/config.py (29 lines) has NO defaults table, only setattr loop + guidance_scale backfill. Entry points: valid_predictor.py:85+128, diffusion_planner_ros node:77-78, onnx_export.py:326-328 (via torch2onnx.py:310), preference_optimization/model_utils.py:65+69, ~20 rlvr tools. Nuance: tier4-main args.json already contain use_velocity_representation/diffusion_model_type; the NEW breakage is specifically diffusion_sample_steps. Fails LOUDLY (crash), not silently. | high |
| C2 | use_velocity_representation default flip False→True + rl_objective default 'official_reward_weighted' | **CONFIRMED / CRITICAL** — both trainers pull argparse defaults from TrainConfig; model built from CLI args, NEVER from resumed checkpoint's args.json (train_grpo_predictor.py:557 then :581 resume; train.py:301 then :311). No layer shapes depend on the flag → strict load_state_dict succeeds → ego latent silently reinterpreted as velocity w/ ego_velocity stats. tier4-main GRPO had NO rl_objective switch → any existing GRPO command re-run on this branch silently trains the official reward-weighted objective. | critical |
| C3 | StateNormalizer.from_json KeyError('ego_velocity') for legacy normalization.json | **CONFIRMED but LOUD guard** (informative fail-fast; e.g. vendored HDP-nuplan normalization.json lacks the key). Sub-claim REFUTED: no silent waypoint-stats site exists — both real callers always define the flag; Config reconstructs normalizer from serialized mean/std, never via from_json. | low |
| C4 | 'ego_velocity' leaks into ObservationNormalizer + args.json | **CONFIRMED but benign** — runtime __call__/inverse skip unknown keys; no data pipeline produces that input key; ROS only applies normalizer; cpp_tools never parses args.json; onnx exports no such metadata. Cosmetic args.json pollution only. | low |
| C5 | encoder ego-history slice flip [:6]→[-6:] | **CONFIRMED both counts**: (a) [-6:] is CORRECT — parse_rosbag.py:168 't=0 oldest, t=N-1 reference time'; neighbor slice was already [-6:]; tier4-main's [:6] kept the OLDEST 6 = genuine bug this branch FIXES. (b) Silent pre-flip checkpoint incompatibility REAL — no config field records slice convention; load checks only state-dict keys; pre-flip checkpoints load cleanly but see learned slots zeroed. | medium |
| C6 | getattr fallback divergence (rl_objective 'grpo' vs config default; beta getattr'd but normalize direct) | **REFUTED as active bug** — all real callers supply the fields (single caller train_grpo_predictor defines all argparse flags; no Config-backed namespace reaches these functions). Latent hazard/inconsistency worth a cleanup note only. | low |

### V4 export/guidance — COMPLETE
| id | Finding | Verdict | Severity | Key evidence |
|----|---------|---------|----------|--------------|
| D1 | TurnIndicatorONNXWrapper doubly broken | **CONFIRMED** — (a) onnx_export.py:215 plain torch.mean vs decoder.py:681-684 masked mean (commit 795a76d touched ONLY decoder.py; ONNX wrapper never updated; ~564 tokens mostly padding → several-to-10x attenuation; affects BOTH waypoint AND velocity checkpoints). (b) :216-218 raw latent slice = non-velocity branch; velocity path needs integrate→re-normalize→slice. Auto-exported next to EVERY checkpoint (train.py:529/556); torch2onnx.py:197 validates against the same broken wrapper (self-consistent, never caught). FullONNXWrapper unaffected; split-graph consumers hit. | high |
| D2 | DecoderONNXWrapper raw DiT output, no model_type conversion / no velocity integration in split-graph contract | **PLAUSIBLE** — default x_start checkpoints are fine (raw output IS x0); risk contingent on external C++/TensorRT consumer implementing pre-velocity contract (test_delay_onnx.py:138 semantics); noise/score/v exports silently mislabeled 'final_x0'. No in-repo consumer of split decoder graph (cpp benchmark_tool + ROS node use FULL graph, which traces _latent_to_prediction correctly). | medium |
| D3 | Guidance broken in velocity mode + non-x_start | **CONFIRMED** (with correction: NO 40x scale error — composer's inverse IS row-aware w/ ego_velocity stats; the break is SEMANTIC: x_phys[:,0] = per-step displacements treated as positions by all energies (collision COG offsets etc.); cumsum integration + its transpose in the chain rule (composer.py:75-76) entirely missing; for noise/score/v the 'x_start correction' is eps/score/v garbage). Reachability: OFF by default (Config guidance_fn=None → uncond); actively populated in rlvr grpo_sampler(_batched)/batched_rollout + preference_optimization when use_* flags set → broken for velocity/non-x_start checkpoints through those flows. | high |
| D4 | CollisionGuidance.reward() autograd under @torch.no_grad | **CONFIRMED** — collision.py:156-157 decorator → _compute:118 autograd.grad always raises. Base-class BaseGuidance.reward also @no_grad (same landmine for any _compute using internal autograd). Currently UNREACHABLE in production (only caller is a test using lateral/longitudinal fns; grpo collision reward uses separate compute_collision_reward path) — armed landmine the composer docstring invites. | medium |
| D5 | guidance_wrapper.py:74-76 all-NaN energy fallback zeros without graph → cond_grad_fn RuntimeError aborts planning | **CONFIRMED** — contrast composer.py:80 surrogate keeps graph (graceful zero grad). Narrow: needs GuidanceWrapper (preference_optimization legacy, flags default False) + all energies NaN simultaneously. | low |
| D6 | Delay/prefix contract broken in velocity mode | **CONFIRMED** — training pins prefix in VELOCITY latent space (decoder.py:119-121,136); inference pins caller's tensor verbatim (:545-558) then cumsum-integrates ego row (:434-438); in-repo executable spec is waypoint-space (test_delay.py:121/test_delay_onnx.py:138) → output[:delay]==prefix does NOT hold for delay>1 on velocity checkpoints. Scope: every in-repo runtime producer uses delay=0 (grpo randn·scale, rlvr randn·temperature, reproducer zeros, validate zeros) where index 0 = current state is space-consistent → breakage confined to the latency-compensation feature (external C++ node + delay tests). No velocity-aware prefix encoder exists anywhere. | medium |
| D7 | dpm_solver:509-514 timesteps_masked no-op | **CONFIRMED** — byte-identical get_time_steps args; torch.where is exact no-op. Git: commit 0b66691 changed training to randomized scaled prefix time and presumably intended a scaled inference schedule but duplicated call degenerates to no pinning. Mitigation: mask_coeff~U[0,1] training covers t_masked→t boundary, so inference sits at the EDGE (not outside) of training distribution. Dead code + delay-path-only skew. | low |

### V7 data pipeline — COMPLETE
| id | Finding | Verdict | Severity | Key evidence |
|----|---------|---------|----------|--------------|
| G1 | interpolation_future_trajectory 3-col-only, unconditional | **CONFIRMED as LATENT crash** — runs unconditionally (:195) even when aug_flag all-False; 3-col-only (:555-566 reads ch2 as heading, :610 emits 3-col, :613 concat (B,20,3)+(B,60,4) → RuntimeError). Diff's new 4-col centric_transform branches (:398-412,:429-438) are UNREACHABLE via __call__ with 4-col data. Tension resolved: LIVE corpus is 3-col (verified npz: ego_agent_future (80,3) float32; HDP velocity repr is decoder-side torch.diff, orthogonal to npz width; convert_cpp_bin_to_python_npz still down-converts 4→3). So current training fine; quintic aug + any 4-col corpus (rlvr cpp_bin_to_npz.py, scene-gen) = first-batch crash. | medium |
| G2 | goal_pose never transformed under augmentation | **CONFIRMED HIGH (live)** — zero 'goal' matches in both aug files; goal_pose IS a model input (encoder.py:205,:247,:262 GoalPoseEncoder); defaults use_data_augment=True augment_prob=0.5 (live args.json confirms); ~50% of moving-ego samples train with goal in pre-perturbation frame while route/lanes rotate ±0.2 rad / shift ±0.75m → ~20m lateral inconsistency at d=100m. Model trained to mistrust goal token. BASE bug (pre-existing). | high |
| G3 | Neighbor future misalignment (both converters) | **CONFIRMED HIGH — EMPIRICALLY VERIFIED ON LIVE CORPUS**: across 50 sampled npz, 747/747 short-tracked neighbor futures have future[0] xy EXACTLY equal to past[-1] xy → index 0 = t+0.0 (ego future starts t+0.1); whole neighbor GT one step (0.1s) late + duplicates an input frame; ~1m systematic error at 10 m/s on every supervision point of every neighbor not persisting all 80 frames. parse_rosbag.py:365-388 deque seeding + neighbor_processor.cpp:160-165 identical. BASE bug. | high |
| G4 | parse_rosbag float64 ego arrays | **CONFIRMED conditional** — only bites npz produced directly by ros_scripts/parse_rosbag.py (loader casts only unsigned ints; bmm float32×float64 crash, loud). Live corpus is float32 (C++ pipeline) → no current impact. | low |
| G5 | pose_padding_mask after heading_to_cos_sin | **CONFIRMED low** — live ego_agent_past 3-col so (0,0,0)→(0,0,1,0), mask never fires, transform corrupts padded rows; 15/200 live npz (7.5%) have all-zero ego-past rows; aug-specific corruption sub-meter; (0,0,1,0) pseudo-pose itself a pre-existing representation quirk. | low |
| G6 | ObservationNormalizer.inverse mask on normalized data | **CONFIRMED as asymmetry, practically theoretical** — __call__ re-zeroes padding after normalization so padding round-trips correctly; wrong-zero needs raw==mean elementwise across 12+ channels in float32 (measure-zero). | low |
| G7 | BridgeStatePerturbation | **CONFIRMED both parts** — (a) same goal_pose omission (live defect when bridge selected); (b) 4-col futures: cos overwritten as angle, sin untouched (:1421-1445), and (T,3) assign into (T,4) slot crashes first (:1379) for augmented samples. Selectable (--augment_type bridge) but non-default; live corpus 3-col. | medium |

### V9 rlvr compat + cleanup facts — COMPLETE
| id | Finding | Verdict | Severity | Key evidence |
|----|---------|---------|----------|--------------|
| I1 | rlvr/preference stacks corrupt velocity checkpoints | **CONFIRMED** — rlvr/grpo_loss.py:76-78+119-120, grpo_sft_trainer.py (_compute_sft_diffusion_loss), preference_optimization/dpo_loss.py:47-49+119 all build ego GT as normalized WAYPOINTS using the checkpoint's normalizer row 0 — which for HDP checkpoints holds VELOCITY stats (std 0.5 vs neighbor 20 = exactly 40x) — and MSE against a model that outputs normalized VELOCITY. Zero grep hits for use_velocity_representation under rlvr/ or preference_optimization/ (no guard). Stacks live (commits through 2026-07-01). Every new checkpoint triggers (default True). | high |
| I2a-i | Cleanup facts | **ALL CONFIRMED** — (a) diffusion_prediction_target/_to_x_start dead (zero call sites repo-wide); (b) hybrid_loss zero callers; (c) l_v discarded by both callers AND == ego_diffusion_loss element-for-element under default x_start (pure waste); (d) _latent_to_prediction current_states unused; (e) decoder.py:269 inline denorm = exact semantic duplicate of helper used 3 lines earlier; (f) _train_config_default byte-identical dup; (g) three dim-expansion helpers; (h) marginal_prob(ones_like) materializes [B,321,80,4] where marginal_alpha would broadcast; (i) wandb step-log dict correctly guarded (only built when logging) — but compute_grad_stats runs every step regardless (aside). | low |
| I2j | diffusion_sample_steps 25 vs 10 | **CONFIRMED medium** — decoder.py:592/358 + train_config.py:118 =25 (args.json of HDP runs confirms); official reference sampling.py:10 diffusion_steps=10; pre-HDP merge-base decoder hardcoded steps=10. NFE = steps+1 (denoise_to_zero) → 26 vs 11 ≈ 2.4x inference cost raised BY THIS BRANCH, unjustified anywhere. | medium |
| I3a-c | load_weights_only concerns | **ALL REFUTED** — (a) prefix reconciliation correct for all 4 combinations (unexpected keys carry model convention; both allowlist spellings live); (b) base→HDP loads cleanly: HDP adds NO state-dict entries (plain attrs only; note HEAD~ briefly had register_buffer at decoder.py:309/314 but working tree removed them); (c) torch 2.11 pinned → torch.load defaults weights_only=True (explicit arg = hygiene only; real pickle surface: preference_optimization/model_utils.py:43 weights_only=False). | low |
| I4 | epoch-0 wandb block | **REFUTED** — inside rank-0 + use_wandb guards; WANDB_MODE=offline fallback prevents crash; stepless log harmless. | low |

### V6 grpo_utils RL — COMPLETE
| id | Finding | Verdict | Severity | Key evidence |
|----|---------|---------|----------|--------------|
| F1 | compute_official_reward_weights defects | **CONFIRMED all 3 sub-claims** — (a) 'batch' normalize: mean/std on RAW rewards pre-filter; one NaN/inf → all reward_norm NaN → nan_to_num zeros → weights=exp(0)=1 → silent unweighted BC (non-default mode). (b) per-group std>eps gate applied for ALL normalize modes; official HDP reference (dp_vla_rl_agent.py:632-660) has NO std gate — zero-variance groups KEPT at weight exp(0)=1. Deviation: grpo_noise_scale=0 ('deterministic') → every group zero-variance → ALL weights 0 → silent RL no-op. (c) n=1 → std NaN → all invalid → loss 0, silent no-op (default n=8). | medium |
| F2 | compute_group_advantages zero-variance/n=1 | **REFUTED as bug** — zeroed advantages = standard GRPO for zero-signal groups; NaN double-caught (nan_to_num + valid gate). Nit: num_generations=1 silently degenerates to no-op instead of raising (validation gap). | low |
| F3 | Rollout init skew | **CONFIRMED** — three coexisting init distributions: RL rollouts randn·U[0,3] (grpo_noise_scale=3.0), validation/deployment ZEROS, theoretical VP prior N(0,1) (alpha(1)=0.00657, std=0.99998). Official HDP is also off-prior but CONSISTENT (randn·0.5 for BOTH rollouts and deployment). U[0,3] documented as intentional diversity — mitigates intent, not the skew. | medium |
| F4 | compute_gt_l2_distance stationary hole + sparse collision reward | **CONFIRMED** — all-zero GT → valid.sum()=0 → clamp → ADE=0 → w_gt_l2 realism anchor vanishes exactly in stop scenes (futures ARE ego-centric per frame_filters.hpp:24; trigger needs |x|+|y|<=1e-6 — bit-frozen poses; sub-mm jitter escapes; structurally conflates stationary with padded). PLUS: collision penalty evaluated only at _NEIGHBOR_EVAL_STEPS [0,20,40,60,79]; cummax carries only DETECTED hits → collision entered+exited between eval steps contributes ZERO penalty. | medium |
| F5 | ego_pseudo_gt frame | **REFUTED as bug** — consistently ego-centric end-to-end (prediction = inverse-normalized ego-centric; no world transform anywhere; aug re-centers both obs and futures). 'world frame' docstrings (grpo_utils.py:75,114,441) are misleading naming only → doc fix. | low |
| F6 | SFT vs RL ego time-0 slot consistency | **REFUTED** — decoder.py:117-121 and grpo_utils.py:322-328 line-for-line identical (obs-normalized current state at t=0, velocity overwrite only indices 1:, identical xT construction). | low |

### V8 base model path — COMPLETE
| id | Finding | Verdict | Severity | Key evidence |
|----|---------|---------|----------|--------------|
| H1 | xT init zeros at eval/deployment | **CONFIRMED** — validate_model.py:252 zeros (valid_predictor inherits); solver has ZERO randn calls (pure ODE from given x) → deterministic, off-marginal (||x||≈0 vs sqrt(324)≈18). Census: zeros in validate/preference/rlvr-batched(noise_scale=0)/reproducer/tensor_converter; randn·U[0,3] grpo; randn·0.5 cpp benchmark; ROS node builds NO sampled_trajectories (pytorch path would KeyError; ONNX delegates to external caller). Documented intent ('Matches the C++ production node'), but validation measures ONE deterministic mean-seeded plan and RL rollouts are scored under a different init than eval/production. | medium |
| H2 | longitudinal_velocity post-normalization no-op | **CONFIRMED** (dup E6; also feeds NEIGHBOR rows via weighted_waypoint_dpm_loss in both branches + grpo_utils.py:295; pre-existing at base 4956206). | medium |
| H3 | Inference neighbor rows unmasked garbage | **CONFIRMED, low blast radius** — neighbor_current_mask computed (decoder.py:399) but never applied to returned prediction (:597-599, flow_matching :517-518 same); invalid rows ≈ 10+20·latent. Loss side IS fixed in this fork (:243). Consumer without re-mask: preference_optimization/visualization.py:88-95 (phantom trajectories in epoch visualizations). Important consumers re-mask (validate metrics, visualize_prediction skips, ROS uses ego row only, rlvr losses mask). | low |
| H4 | dit.py:123 T=81 hardcode vs config.future_len output_dim | **CONFIRMED** — config-hygiene only (future_len pinned at 80 everywhere). Pre-existing. | low |
| H5 | flow_matching inference crash | **CONFIRMED** — ode_solver passes 3-D x into DiT assert x.dim()==4 (dit.py:156). At base it failed on BOTH x and t asserts; HDP diff fixed only the t side. Never worked; dormant path (no config uses flow_matching). | low |
| H6 | Pooling div-zero / encoder masking | **REFUTED** — clamp_min(1) guards pooling; ego token never masked; 37e511d encoder output masking present and correct. Adjacent re-confirm: onnx_export.py:215 UNMASKED mean (see D1). | low |

### V5 decoder loss math — COMPLETE
| id | Finding | Verdict | Severity | Key evidence |
|----|---------|---------|----------|--------------|
| E1 | Turn-indicator input = waypoints normalized by ego_velocity std 0.5 (×40 vs waypoint mode; features reach ~240-480 vs ~4-11) | **CONFIRMED** — decoder.py:117 (waypoint_gt via norm whose ego row = velocity stats) → :142 turn_indicator_trajectories → :465 slice; inference consistent (:429-431, same mis-scale). Bare nn.Linear head, no LayerNorm (:342-344, 416-417). Warm start loads waypoint-trained head weights silently (identical shapes, strict=False). Bounded: head is auxiliary (GT-input path carries no grad to trunk) → damage = turn-indicator quality + encoding_pooled gradient noise. | medium |
| E2 | Prefix columns (delay≤5) for supervision noise/v/score target the never-applied z; ~95x sigma amplification worst-case (1/sigma(eps)=95.4) for x_start→noise cross-supervision | **CONFIRMED** — xT=where(prefix_mask, all_gt, xT) decoder.py:136; targets z at :163-166 (score :159/193/210); NO masking excludes prefix columns from ego loss (:252 mean over horizon 80 includes cols 1..delay). Verbatim duplicate in grpo_utils.py:340/365-368/383-411. Default x_start UNAFFECTED (target=input, trivially learnable). Damage = amplified gradient noise + irreducible loss floor in the noise/v/score experiment arms. | high |
| E3 | pred_x_start 1/alpha amplification (1/alpha(1)=152) for model_type noise/score/v feeding always-on hybrid waypoint loss (cumsum 80 steps, squared) + penalties, no SNR weighting | **CONFIRMED** — at init, pred_x_start at t~1 = gt + (sigma/alpha)z ≈ 76 m/step noise → hybrid loss ~1e5-1e6 dominating every O(1-10) term even after omega=0.01; grad clip bounds magnitude not direction. Penalties themselves bounded (relu margin ≤0.25, SAT depth bounded). NOTE: inherited from official HDP design (reference loss.py:74-77 identical, no SNR weighting); default x_start unaffected (pred_x_start=model_output). Landmine for the noise/score/v arms only. | high |
| E4 | Velocity-branch neighbor x_start supervision uses UNMASKED waypoint_gt | **REFUTED as loss leak** — decoder.py:243 masked_prediction_loss = dpm_loss[:,1:,:][neighbors_future_valid] filters per-(b,p,t) BEFORE mean; weighted_waypoint_dpm_loss strictly per-step, no cross-timestep mixing, no NaN source. Residual = consistency smell only (masked all_gt vs unmasked waypoint_gt). | low |
| E5 | ego_velocity isotropic std 0.5 under-weights lateral per-step error (100-10000x less than longitudinal in squared loss) | **PLAUSIBLE (tuning risk, not defect)** — stats verbatim official HDP; heading channels carry lateral direction at full weight; hybrid l_wpt supervises meters on both axes (comparable to waypoint-mode lat weight) but gradient truncated to W=10 window. Watch lateral-error metrics. | low |
| E6 | decoder.py:88 longitudinal_velocity extracted AFTER ObservationNormalizer (channel-4 std=20) → clamp_min(|v/20*1.0|,1.0) = 1.0 below 20 m/s raw → high-speed lon-loss attenuation never activates on urban data; at 30 m/s divides by 1.5 not ~30 | **CONFIRMED, PRE-EXISTING BASE BUG** — identical in base 4956206 AND sibling repo /mnt/nvme/Diffusion-Planner decoder.py:86. Silently dead feature; fixing changes long-standing behavior → needs deliberate decision. Also: in velocity-mode x_start branch the weight now only applies to NEIGHBOR rows using ego speed. | medium |
